@model IEnumerable<JobSearchBoard_A00218328_Amritpal.Models.Jobs>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JobName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobRequirements)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.JobName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.JobRequirements)
                </td>
                @if (User.Identity.IsAuthenticated == true && !string.IsNullOrEmpty(User.Identity.Name))
                {
                    <td>
                        @*<a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.ID">Delete</a> |*@
                        <a asp-action="Create" asp-controller="UserApplieds">Apply for Job</a>
                    </td>
                }
            </tr>
        }
    </tbody>
</table>

